import os

# Returns a list of all files and folders in the current working directory.
print(os.listdir())

#os.get_blocking() is a low-level operating system function used to check whether a file descriptor is in blocking or non-blocking mode.
print(os.get_blocking())

#os.chdir() is used to change the current working directory of your Python program.
print(os.chdir("/"))